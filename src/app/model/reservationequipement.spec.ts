import { Reservationequipement } from './reservationequipement';

describe('Reservationequipement', () => {
  it('should create an instance', () => {
    expect(new Reservationequipement(null)).toBeTruthy();
  });
});
